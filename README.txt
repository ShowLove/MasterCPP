//Start at top directory in my case 
~/Desktop/test folder/advancedCPP

//The first command will creates CMake configuration files inside folder build
cmake

//the second one will generate the output program 
make -j4

//the third command runs the code
./advancedCPP
